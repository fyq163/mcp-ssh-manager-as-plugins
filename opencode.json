{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "mcp-ssh-manager": {
      "type": "local",
      "command": ["node", "./src/index.js"],
      "enabled": true,
      "environment": {
        "SSH_CONFIG_PATH": "~/.config/mcp-ssh-manager/ssh-config.toml"
      }
    }
  }
}
